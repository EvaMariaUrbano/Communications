clc; clear;
N_sat=18;
N_planes=9;
h=545;
I=15;
mu=26;
t=0:10:24*2*60;
e_min=10;
lambda=75:5:90;
long=0:45:180;
lat=length(lambda);
l=length(t);

Xs=Orbital_position(N_sat,N_planes,h,I,mu,t);
L=zeros(lat,l);

for i=1:lat
    Xg=Ground_position(lambda(i),longt);
    [L(i,:)]=Links(Xs,Xg,e_min);
end

%%
figure; plot(t/60,L); legend 75º 80º 85º 90º; xlabel('time (h)'); ylabel('links');
